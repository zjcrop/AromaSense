name: Manual AromaSense Web Deploy B0.1.a

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: aromasense-production
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-24.04
    env:
      AROMASENSE_CLOUD_URL: ${{ vars.AROMASENSE_CLOUD_URL }}
      AROMASENSE_FIREBASE_API_KEY: AIzaSyAsY_w3pxgBlnr0tFYKuAvNJUeEhN1RCU0
      AROMASENSE_FIREBASE_PROJECT_ID: romasense-f23eb
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node 24
        uses: actions/setup-node@v7
        with:
          node-version: '24'

      - name: Validate manual connected configuration
        shell: bash
        run: |
          set -euo pipefail
          test -n "${AROMASENSE_CLOUD_URL:-}" || (echo "Manual Pages deployment requires repository variable AROMASENSE_CLOUD_URL." && exit 1)
          case "$AROMASENSE_CLOUD_URL" in
            https://*) ;;
            *) echo "AROMASENSE_CLOUD_URL must use https://"; exit 1 ;;
          esac

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Build browser version
        run: npm run bundle:web

      - name: Validate Pages artifact
        shell: bash
        run: |
          set -euxo pipefail
          test -f site/index.html
          test -f site/app.js
          test -f site/sql-wasm.wasm
          ! grep -q '__CLOUD_BASE_URL__\|__FIREBASE_API_KEY__\|__FIREBASE_PROJECT_ID__' site/index.html
          grep -Fq "data-cloud-base-url=\"$AROMASENSE_CLOUD_URL\"" site/index.html
          grep -Fq "data-firebase-api-key=\"$AROMASENSE_FIREBASE_API_KEY\"" site/index.html
          grep -Fq "data-firebase-project-id=\"$AROMASENSE_FIREBASE_PROJECT_ID\"" site/index.html

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-24.04
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
